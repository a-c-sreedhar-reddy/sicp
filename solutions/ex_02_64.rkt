; partial-tree takes a list and recursively divides in to two groups
; then it creates a tree from left group and another tree from right group
; and then it creates a new tree combining both of them.

;	 	5
;	1		9
;	    3 	     7     11	


; b) order of growth is n;